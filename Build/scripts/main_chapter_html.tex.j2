% !TEX TS-program = arara
(# Jinja2 template for per-chapter HTML build via lwarp.          #)
(# Rendered by build_chapter_html.py from chapter metadata.       #)
% arara: clean: {files: ['main-authors.aux'] }
(# Images and bib are already collected by the management command    #)
(# before arara runs — no copyImageFiles or mergemainbibfiles here.  #)
% arara: pdflatex: { shell: yes, interaction: batchmode }
% arara: makeindex: { style: StyleInd.ist, output: ind, log: ilg, input: idx }
% arara: biber
% arara: lwarpmk: { options: ['print'] }
% arara: lwarpmk: { options: ['printindex'] }
% arara: lwarpmk: { options: ['html'] }
% arara: lwarpmk: { options: ['html'] }
% arara: lwarpmk: { options: ['htmlindex'] }
% arara: lwarpmk: { options: ['html'] }
% arara: lwarpmk: { options: ['clean'] }
% arara: clean: { extensions: [aux, bbl, bcf, blg, idx, ilg, ind, log, ptc, toc]}
% arara: clean: { files: ['gitHeadLocal.gin', 'main.run.xml'] }
%
% Generated by OpenChapters web — per-chapter HTML build
% Chapter: (( chapter_title ))
% Chabbr:  (( chabbr ))
% Date:    (( build_date ))

\documentclass[11pt,fleqn,A4paper]{book}

\usepackage{iftex}
\ifPDFTeX
\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\else
\usepackage{fontspec}
\fi

\usepackage[
 makeindex,
 ImagesDirectory=ImageFolder,
 HomeHTMLFilename=index,
 HTMLFilename={node-},
 latexmk,
 mathjax,
]{lwarp}

\usepackage{OpenChaptersHTML}
\usepackage{colortbl}  % \cellcolor, \rowcolor, \columncolor
\usepackage{arara}

\setcounter{tocdepth}{2}
\setcounter{secnumdepth}{2}
\setcounter{FileDepth}{0}
\booltrue{CombineHigherDepths}
\boolfalse{FileSectionNames}
\setcounter{SideTOCDepth}{1}

\HTMLTitle{(( chapter_title ))}
\HTMLAuthor{(( authors ))}
\HTMLLanguage{en-US}
\HTMLDescription{(( description ))}
\CSSFilename{lwarp_sagebrush.css}

% OpenChaptersHTML.sty already defines \chaptergraphicspath as ImageFolder/
% and uses it when including chapter images. Set \graphicspath (used as a
% text macro in chapter source) to empty so the path isn't doubled.
\renewcommand{\graphicspath}{}
\newcommand{\isHTML}{Yes}
\newcommand{\isEPUB}{No}

\input{preambleHTML.ins}

% Per-chapter bibliography
\addbibresource{chaptercitations.bib}

\begin{document}

\tableofcontents

% Open the authors file for writing
\immediate\openout\authorfile=\jobname-authors.aux%

\include{(( include_path ))}

\printbibliography

\end{document}
