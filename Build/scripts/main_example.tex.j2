% !TEX TS-program = arara
(# Jinja2 template for a worked-example snippet preview.              #)
(# Rendered by build_example_preview.py from Example record.          #)
(# Body is a minimal Statement + Solution, no chapter content.        #)
% arara: pdflatex: { shell: yes, interaction: batchmode }
% arara: pdflatex: { shell: yes, interaction: batchmode }
% arara: clean: { extensions: [aux, bbl, bcf, blg, log, ptc, toc, run.xml]}
%
% Generated by OpenChapters web — worked-example snippet preview.
% Example: (( example_id ))
% Primary chapter: (( primary_chabbr ))
% Date:    (( build_date ))

\documentclass[11pt,fleqn,A4paper]{book}

\usepackage{OpenChapters}
\usepackage{arara}

\renewcommand{\chaptergraphicspath}{ImageFolder/}
\renewcommand{\graphicspath}{ImageFolder/}
\newcommand{\isHTML}{No}
\newcommand{\isEPUB}{No}

\input{preamble.ins}

% No chapter header image for the snippet preview — the OpenChapters
% \chapter rendering otherwise calls \includegraphics{} with an empty
% argument and aborts the build.
\chapterimage{\noheaderimage}

\begin{document}

\chapter*{Worked example (( example_id ))}
\addcontentsline{toc}{chapter}{Worked example (( example_id ))}

\section*{Statement}
(( statement_tex ))

\section*{Solution}
(( solution_tex ))

\end{document}
